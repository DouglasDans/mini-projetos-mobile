import React from 'react'
import { ScrollView, Text, View } from 'react-native'
import styles from './index.style'

type Props = {}

export default function GameDetails({ }: Props) {
  return (
    <ScrollView style={styles.container}>
      <Text>sdjbfisdbf</Text>
    </ScrollView>
  )
}